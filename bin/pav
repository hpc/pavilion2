#!/bin/bash

PYTHON=$(which python3)

PAV_DIR=$(dirname $(dirname $(realpath ${BASH_SOURCE[0]})))

export PYTHONPATH="${PYTHONPATH}:${PAV_DIR}/lib:${PAV_DIR}/lib/pavilion/dependencies"

${PYTHON} ${PAV_DIR}/bin/pavilion.py $@
