# YAML
#
# Input for running a SNAP input that scales up to the size of the computer as necessary.

SNAP:
  name: SNAP

  source_location: /users/sly/git/SNAP/src

  run:
    cmd: 'run_it.sh'
    scheduler: slurm

  slurm:
    num_nodes: [ 'all' ]
    time_limit: 01:00:00

  splunk:
    state: off
