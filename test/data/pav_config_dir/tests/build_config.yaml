fine:
    build: 
        source_location: '.'
        source_download_name: 'has_location.tar.gz'
        cmds: 'echo this'

not_fine:
    build:
        source_download_name: 'no_location.tar.gz'
        cmds: 'echo this'
