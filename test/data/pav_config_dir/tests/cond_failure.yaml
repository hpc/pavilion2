one:
    only_if:
        sys_name: ['Bob','joe','toad']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
two:
    not_if:
        sys_name: ['Bob','joe','bieber']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
three:
    only_if:
        sys_name: ['Bob','Eggman','bieber']

    not_if:
        sys_host: ['windows98','centos']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
four:
    only_if:
        sys_host: ['windows98','centos']
        sys_name: ['Trinity', 'mehhh']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
five:
    only_if:
        sys_name: ['Bob','bieber']

    not_if:
        sys_arch: ['x1000000']
        sys_host: ['centos']
    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
six:
    only_if:
        sys_host: ['Turkey','Moldova','Ukraine']
        sys_name: ['bieber']

    not_if:
        sys_arch: ['aarch64']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
seven:
    not_if:
        sys_name: ['hello_there','False']
        sys_arch: ['x86_64']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
eight:
    only_if:
        sys_host: ['centos']
        sys_name: ['wooord','MMMmm']

    not_if:
        sys_name: ['festering']
        sys_arch: ['aarch64','x86_64']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
