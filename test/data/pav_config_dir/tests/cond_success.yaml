one:
    only_if:
        sys_name: ['bieber','calvin']
    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
two:
    only_if:
        sys_name: ['Trinity','macbook','bieber']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
three:
    only_if:
        sys_name: ['Bob','Eggman','bieber']

    not_if:
        sys_arch: ['haswell','arrch64']

    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
four:
    only_if:
        sys_name: ['bieber','Trinity','macbook']

    not_if:
        sys_arch: ['haswell','aarch64']
    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
five:
    not_if:
        sys_arch: ['haswell','aarch64']
    scheduler: raw
    raw:
        concurrent: False
    build:
        cmds:
            - 'echo "building test"'
    run:
        cmds:
            - 'echo "test completed"'
