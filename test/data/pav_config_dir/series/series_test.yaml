series:

    initial_tests:
        tests: ["echo_test.a", "echo_test.b"]
        modes: ["smode1"]

    next_tests:
        tests: ["echo_test.d"]
        depends_on: [initial_tests]

    last_tests:
        tests: ["echo_test.wrong_year"]
        only_if:
            '{{year}}': 1997

modes: ['smode2']
